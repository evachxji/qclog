# 2026-04-14

## 将暖色Vlog前端工程转换为微信小程序

- 原工程：`d:/Document/trae/qclog/index.html` + `img/aig/` 9张图片，为静态相册展示页。
- 转换产物：`d:/Document/trae/qclog/miniprogram/`，包含完整小程序结构。
  - `app.js / app.json / app.wxss / sitemap.json`
  - `pages/index/index.wxml/.wxss/.js/.json`
  - `img/` 图片资源原样复制
- 主要适配点：CSS px → rpx、`<img>` → `<image mode="aspectFill">`、点击卡片调用 `wx.previewImage` 全屏预览、支持转发分享。
- 部署说明写入 `miniprogram/README.md`。
